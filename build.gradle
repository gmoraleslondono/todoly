/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at https://docs.gradle.org/4.10.1/userguide/tutorial_java_projects.html
 */

plugins {
    // Apply the java plugin to add support for Java
    id 'java'

    // Apply the application plugin to add support for building an application
    id 'application'
}

// Define the main class for the application
mainClassName = 'com.gmoraleslondono.todoly.App'

// Java version compatibility to use when compiling Java source.
sourceCompatibility = 1.8

dependencies {
    // This dependency is found on compile classpath of this component and consumers.
    compile 'com.google.code.gson:gson:2.8.7'

    // Use JUnit test framework
    testCompile 'junit:junit:4.13'
}

// In this section you declare where to find the dependencies of your project
repositories {
    // Use jcenter for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
}

// Set the standard input stream for the process
run {
    standardInput = System.in
}

version = '1.0.0'

jar {
    manifest {
        attributes 'Main-Class': mainClassName,
            'Implementation-Title': 'ToDoly',
            'Implementation-Version': version,
            'Implementation-Vendor': 'https://github.com/gmoraleslondono'
    }

    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}
